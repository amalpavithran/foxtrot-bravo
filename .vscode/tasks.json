{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "vsls",
            "taskUid": "flutter:flutter pub run build_runner build",
            "problemMatcher": [
                "$vsls"
            ],
            "group": "build",
            "label": "flutter:flutter pub run build_runner build"
        }
    ]
}